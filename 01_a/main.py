from unidade_federativa import Unidade_Federativa

estados = Unidade_Federativa.get_lista_uf()
estados0 = Unidade_Federativa.get_uf()

for indice, estado in enumerate(estados):
    print(estados[0, 1])
